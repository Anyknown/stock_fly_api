name: Run Generate Table
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Python script
        run: |
          python alpha_v_stocktrack.py
